pass classloader conversion around as strategy to avoid performance impact
collapse mess of finders into one composite junit finder + revisit/remove canHandle method
strategy for grouping

move before after class our of basic test unit finder and into processor - be carefull before class not run twice for adapted junit tests

support for BDD (fit protocol??) by returning return values in meta data

mutation tests suite where tests to run selected from an initial code coverage run. Follow drive form the classes model rather than drive form the tests (support both)

Test historian.  