pass classloader conversion around as strategy to avoid performance impact
collapse mess of finders into one composite junit finder + revisit/remove canHandle method
strategy for grouping

move before after class our of basic test unit finder and into processor

distributed mutation testing works, but starting hazelcast for each process is expensive. Can the slave
not just read the parent's cache? Parent would need to run the unmutated tests first to pull in required data.

  